# Використовуємо офіційний Node.js образ як базовий
FROM node:18-alpine

# Встановлюємо робочу директорію в контейнері
WORKDIR /usr/src/app

# Копіюємо package.json та package-lock.json до робочої директорії
COPY package*.json ./

# Встановлюємо залежності
RUN npm install

# Копіюємо весь код проекту до робочої директорії
COPY . .

# Відкриваємо порт 3000 для доступу до додатку
EXPOSE 3000

# Команда для запуску проекту в режимі розробки
CMD ["npm", "run", "dev"]
